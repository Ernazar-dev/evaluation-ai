{
  "name": "ai-assistant-platform",
  "version": "1.0.0",
  "private": true,
  "description": "AI Assistant Platform — Node.js + PostgreSQL + React (Vite/Tailwind/AntD)",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build && npm --prefix backend install --include=dev && npm --prefix backend run prisma:generate",
    "build:server": "npm --prefix backend install --include=dev && npm --prefix backend run prisma:generate",
    "db:push": "npm --prefix backend run prisma:push",
    "db:seed": "npm --prefix backend run seed",
    "seed": "npm --prefix backend run seed",
    "start": "npm --prefix backend start",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
